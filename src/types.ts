export type Item = {
    item: string;
    store: string;
    timestamp: string;
    uuid: string;
  };

  export type ShoppingList = {
    item: string;
    store: string;
    timestamp: string;
    uuid: string;
  }

  export type StateType = {
    item: string;
    store: string;
    timestamp: string;
    uuid: string;
}